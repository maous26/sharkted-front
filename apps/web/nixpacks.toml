[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["rm -rf .next", "npm run build"]

[start]
cmd = "npm start"

[variables]
PORT = "8080"
CACHE_BUST = "20251214-v3"
